
@{
    ViewBag.Title = "Sablon";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

&nbsp;


<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Yazarlar</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li><a href="/Writer/AddWriter" class="btn btn-success">Yazar Ekle</a></li>&nbsp;&nbsp;
                    <li class="breadcrumb-item">Writer</li>
                    <li class="breadcrumb-item active">Index</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card card-solid">
        <div class="card-body pb-0">

            Sayfa içeriğinin yazılacağı alan
            @* value çekilen veri ile aynı olursa Seçili olarak geliyor
        @Html.Label("Durumu :")
        @Html.DropDownListFor(x => x.HeadingStatus,
             new List<SelectListItem> {
                 new SelectListItem {
                    Text="Aktif",
                    Value="True",
                        // Selected = true, seçili olarak gelsin
                    // Disabled = true  disable olarak gelsin istersek kullanıyoruz
                 },
                 new SelectListItem {
                    Text="Pasif",
                    Value="False",
                 },
             }, new { @class = "form-control" })
            *@
        </div>
    </div>
    <!-- /.card -->
</section>
<!-- /.content -->